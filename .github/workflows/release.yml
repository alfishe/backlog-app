name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (e.g., v0.1.0)'
        required: true

env:
  CARGO_TERM_COLOR: always

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          # macOS ARM
          - platform: macos-14
            target: aarch64-apple-darwin
            name: macos-arm64

          # macOS x86_64 (cross-compile on ARM runner)
          - platform: macos-14
            target: x86_64-apple-darwin
            name: macos-x64

          # Windows x86_64
          - platform: windows-latest
            target: x86_64-pc-windows-msvc
            name: windows-x64

          # Windows ARM64
          - platform: windows-latest
            target: aarch64-pc-windows-msvc
            name: windows-arm64

          # Linux x86_64
          - platform: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
            name: linux-x64

    runs-on: ${{ matrix.platform }}

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install Linux dependencies
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf

      - name: Install npm dependencies
        run: npm ci

      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.event.inputs.version || github.ref_name }}
          releaseName: 'Backlog ${{ github.event.inputs.version || github.ref_name }}'
          releaseBody: |
            Cross-platform desktop app for Personal Backlog.

            ## Downloads
            - **macOS ARM**: `Backlog_*_aarch64.dmg`
            - **macOS Intel**: `Backlog_*_x64.dmg`
            - **Windows x64**: `Backlog_*_x64-setup.exe` or `.msi`
            - **Windows ARM64**: `Backlog_*_arm64-setup.exe`
            - **Linux x64**: `backlog_*_amd64.deb` or `.AppImage`

            ## Features
            - Fully offline - no internet required
            - Single markdown file as database
            - Embedded HTTP server for API
            - Cross-platform (Windows, macOS, Linux)
          releaseDraft: true
          prerelease: false
          args: --target ${{ matrix.target }}

      - name: Upload artifacts
        uses: actions/upload-artifact@v5
        with:
          name: backlog-${{ matrix.name }}
          path: |
            src-tauri/target/${{ matrix.target }}/release/bundle/**/*.dmg
            src-tauri/target/${{ matrix.target }}/release/bundle/**/*.app
            src-tauri/target/${{ matrix.target }}/release/bundle/**/*.exe
            src-tauri/target/${{ matrix.target }}/release/bundle/**/*.msi
            src-tauri/target/${{ matrix.target }}/release/bundle/**/*.deb
            src-tauri/target/${{ matrix.target }}/release/bundle/**/*.AppImage
            src-tauri/target/${{ matrix.target }}/release/bundle/**/*.rpm
          if-no-files-found: ignore

  # NixOS build
  nix-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable

      - name: Build with Nix
        run: |
          nix build .#default --print-build-logs

      - name: Create Nix bundle
        run: |
          mkdir -p nix-bundle
          cp -r result/* nix-bundle/ 2>/dev/null || true
          # Create a simple wrapper script
          cat > nix-bundle/install.sh << 'EOF'
          #!/bin/bash
          # Install backlog-app via Nix flake
          echo "Installing backlog-app..."
          nix profile install github:alfishe/backlog-app
          echo "Done! Run 'backlog-app' to start."
          EOF
          chmod +x nix-bundle/install.sh
          tar -czvf backlog-nix-${{ github.ref_name }}.tar.gz nix-bundle

      - name: Upload Nix artifact
        uses: actions/upload-artifact@v5
        with:
          name: backlog-nix
          path: backlog-nix-*.tar.gz
          if-no-files-found: ignore

      - name: Upload to release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.version || github.ref_name }}
          files: backlog-nix-*.tar.gz
          draft: true
