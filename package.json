{
  "name": "backlog-app",
  "version": "0.1.0",
  "description": "Cross-platform desktop app for Personal Backlog",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
